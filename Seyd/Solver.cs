namespace Seyd
{
    internal class Solver
    {
        private MountainData MountainData;

        public Solver(MountainData mountainData)
        {
            MountainData = mountainData;
        }
    }
}
